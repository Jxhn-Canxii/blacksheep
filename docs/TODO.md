TODO

- [x] emotional ledger get by the blacksheep assistant optional input it will pop up in a random interval but dont make it annoying, this is the basis of neural analysis
- [x] create a ledger page where the blacksheep assistant can store the emotional data of the user, it analyze the data and show the user the emotional state of the user, also show the user the emotional state of the user in a graph.
- [x] name the assistant and make a Greeting Line depends on user previous emotions so that it feels that it cares. (Named: Baara)

- [x] implement the emotional ledger on profile page
- [x] create a workaround on vent bubble that is in the same location. we can make it differentiate from the other vents.
- [x] save the ledger on server via API route.

- [x] the assistant must alwayz be on top sometimes the div overlaps it hides him.see feed page it hides the assistant box
- [x] create a color representation for every emotions.
- [x] vent bubble color differ depends on emotion color.

- [x] why is it if i transfer tab it re renders? (Optimized via persistent tab containers)
- [x] update emotion colors in vent forms,feeds,and others to match the assistant colors.


- [x] show also the vent feed the emotional label.
- [x] also if the user wants to record its emotion he/she will just click the assistant to record it.
- [x] why some emotions dont have colors it default to green refer the complete list is on vent forms. assign best color representation.

- [x] zoom the vent map on street level, map vent cards must have emotional level,create a possible advice based on the user emotional state add a 10+ advice per emotion and pick randomly per emotion and remove the third party api call.

- [x] cant acess ledgers. add also on navigations
- [x] move the explore tab on top right next to the profile.use search icon only so the user can search for other users,vents,emotions. (Map now part of Search/Explore page)
- [x] change the global message icon they have the same on signals icon in navigation
- [x] on emotional ledger recorder on assistant choose from the feelings list dont put input box for the user.
